<%= form_for @support_duty do |f| %>
  <%= f.label :user_id, 'Reschedule to' %>
  <%= f.collection_select :reschedulable_id, @support_duties, :id, :choice_option, include_blank: true %>
  <%= f.submit 'Submit' %>
<% end %>